{
  "name": "naming-service-api",
  "version": "0.1.0",
  "private": true,
  "description": "命名サービス API（Vercel Functions・TypeScript）。Phase1: 姓名診断＋LLMコメント。",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "db:check": "node ../db/check-connection.mjs",
    "db:migrate": "node ../db/migrate.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}

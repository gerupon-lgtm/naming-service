# 命名サービス 要件定義書

## 1. 文書情報・改訂履歴

| 項目 | 内容 |
|------|------|
| 文書名 | 命名サービス（姓名診断＋ペット命名提案） 要件定義書 |
| 版数 | 1.11 |
| 作成日 | 2026-07-17 |
| 作成者 | gerupon |
| 承認者 | gerupon（個人開発） |

| 版数 | 日付 | 変更内容 | 変更者 |
|------|------|----------|--------|
| 1.0 | 2026-07-17 | 初版ドラフト（構想たたき台・データ構造設計v8を統合） | gerupon |
| 1.1 | 2026-07-17 | 結果ファイル出力・クリップボードコピー機能を追加、レスポンシブ対応を確定、LLM利用範囲を確定 | gerupon |
| 1.2 | 2026-07-17 | LLM提供元をOllama（自宅サーバー）主体・OpenRouterフォールバックの構成に変更、フォールバック順を指定可能に | gerupon |
| 1.3 | 2026-07-17 | LLMモデルを設定で変更可能とし、初期想定モデルを明記 | gerupon |
| 1.4 | 2026-07-17 | フォールバック機能・順番指定はMVPから実装することを明記。初期モデルをOllama・OpenRouterともgemma4系に統一 | gerupon |
| 1.5 | 2026-07-17 | 応答不可判定基準・フォールバック設定方法に【想定】デフォルトを設定し、設計ブロッカーを解消 | gerupon |
| 1.6 | 2026-07-17 | 姓名診断（F-001）にもLLMコメント生成（F-012）を追加。ロジック計算結果をLLMで肉付けする方式とし、MVP範囲を拡大 | gerupon |
| 1.7 | 2026-07-17 | スコア→ランク閾値、結果ファイル形式（.txt）を確定。kanjiapi.devリトライ・エラー文言、character_masterのsource列追加を確定 | gerupon |
| 1.8 | 2026-07-17 | 姓名判断の流派を熊崎式に確定（将来複数流派対応を見据えた設計とする）。部首の伝統的画数補正に関する制約をリスクに追加 | gerupon |
| 1.9 | 2026-07-17 | Kanji alive APIを使った部首補正の半自動化方針を追加（ビルド時ツールとして利用） | gerupon |
| 1.10 | 2026-07-17 | MuzukanjiAPIをKanji alive対応外の文字を埋める第二候補として追加（仕様は実装時に検証） | gerupon |
| 1.11 | 2026-07-17 | 通し整合性チェック: 機能一覧の優先度にPhase1/Phase2区分を反映、要確認事項を再採番・解消済み分離、参照バージョンを最新化 | gerupon |
| 1.12 | 2026-07-17 | スコア算出を81画吉凶テーブル方式に確定。Phase1（MVP）をローカル実装完了・本番公開（全部Vercel構成）まで到達 | gerupon |
| 1.13 | 2026-07-17 | 性別による吉凶差（F-013）を追加。女性の注意数を吉凶・スコア・解説に反映（伝統準拠、五格計算は不変） | gerupon |
| 1.14 | 2026-07-17 | 診断結果の情報量アップ（F-014）を追加。各格の役割・1〜81画の象意・三才配置（五行）・四柱推命連携用の五行サマリを出力し、LLMコメントの材料を拡充 | gerupon |
| 1.15 | 2026-07-17 | 総合ランクを6段階（大吉/吉/中吉/小吉/末吉/凶）に改定し各下限を設定値化。格に「やさしい呼び名」と成り立ち（構成文字）表示を追加。テキスト出力を分かりやすく刷新。未解決No.1〜3を決定（LLM応答不可=10秒＋4xx/5xx、フォールバック順、非同期コメント）。部首補正の実装状況を明記 | gerupon |
| 2.4 | 2026-07-21 | **F-003（使いたい文字）を再定義**: 単体1文字を名前の**表記**に含める（文字種問わず・かなも表記照合）。**アルファベット対応**のため出力文字種に「ローマ字」（ヘボン式）を追加。使いたい文字×希望のよみが両立しないときは**よみを優先＋お知らせ**（2段構え）。入力欄は1文字制限。詳細は `docs/pet-usable-char.md`。バージョン `mvp-2.4.0` | gerupon |
| 2.3 | 2026-07-21 | **F-003（使いたい文字）の仕様変更**: かなは「よみ」、漢字は「表記」で照合するよう改め、候補を**LLM生成**に切替（機械検証つき）。マスタはフォールバックとして残す。あわせて **F-004** の記述を実装（LLM生成）に合わせて更新（kanjiapi読み逆引きは非採用）。**共有URL（F-006）に用神リスト `wx` を付与**し、生年月日を載せずに五行ボーナスを共有先でも再現できるようにした。候補の由来（マスタ／よみ由来／使いたい文字由来）を区別して表示。バージョン `mvp-2.3.1` | gerupon |
| 2.2 | 2026-07-21 | 五行ボーナスの用語説明をツールチップ化、日付・時刻ピッカーのアイコンをSVG化（スマートフォンで非表示になる不具合の修正）。バージョン `mvp-2.2.0` | gerupon |
| 2.1 | 2026-07-18 | F-015 のUI改善。生年月日を8桁（yyyyMMdd）、出生時刻を4桁（HHmm）の数字入力＋ピッカー併用にし、年に6桁入る問題と分の入力挙動を解消。五行ボーナスの星を**4段階**に拡張し恩恵なしを☆☆☆で表現。度合いのラベルを画面から外し（aria-label には保持）、「補えていません」等の否定的文言を全廃。恩恵なしでも本文を必ず返す。バージョン `mvp-2.1.0` | gerupon |
| 2.0 | 2026-07-18 | **四柱推命連携（F-015）を追加**。生年月日（任意）から用神を求め「四柱推命による五行ボーナス」として別枠表示（3段階の言葉＋星、総合ランク・スコアは不変）。出生時刻・出生地も任意でL1〜L3の積み上げ式。ペット命名モードは対象外。実装にあたり命式計算の精度を精査し、節入り・立春を固定テーブルから天文計算へ変更、日柱オフセットを万年暦で検証、補うべき五行を「最少五行」から**用神論**へ変更。バージョン `mvp-2.0.0` | gerupon |
| 1.16 | 2026-07-17 | シードを本格拡充（常用＋人名用 約3,096字）。部首補正をオフラインパッケージ（kanji-data／kanji）で自動化しRapidAPIキーを不要化（未解決No.4を解消）。UIフォントをBIZ UDP明朝に統一。テキスト出力にLLMコメントを追加。※郎=13画等、熊崎式補正で一部の総画が従来から変化 | gerupon |
| 1.17 | 2026-07-18 | Phase2（ペット命名提案 F-002〜F-005・F-011）とS-003/S-004・タブ切替を実装。提案はユーザー希望（よみ由来）優先＋不足分は条件一致マスタからランダム（件数は設定値）。候補コメントはよみ中心・日本語のみ・段階的・同一よみ統一。かな濁音/半濁音/拗音/促音/々をシード追加（3,168字）。UIフォントをShippori Mincho（500/700）に | gerupon |
| 1.18 | 2026-07-18 | よみ→漢字表記を機械的逆引きからLLM生成方式に変更（自然な表記を生成→先頭2＋画数上位採用）。旧逆引きモジュールは未使用化 | gerupon |
| 1.19 | 2026-07-18 | name_master をDB化（Neonの `name_master` テーブル・配列カラム、DB優先/seedフォールバック）し候補を約216件に拡充。`setup_all.sql` に統合 | gerupon |

## 2. はじめに（背景・目的）

- **背景**: 姓名判断エンジンを実装したアプリを作りたいと思いついたことがきっかけ。診断ロジックを逆利用すれば「良い診断結果になる名前」を提案できるのではという発想から、ペットの命名支援機能に発展した。
- **目的**: 姓名診断（既存の姓名を判定する）機能と、ペット（犬・猫・小動物）向けの命名候補提案機能を持つ無料のWebサービスを、個人開発・低コストで公開する。
- **期待効果**: 【想定】ポートフォリオ・個人開発の実績として活用できる。姓名診断＋ペット命名という組み合わせはSNSでの話題化・拡散も期待できる。

## 3. スコープ（対象範囲・対象外）

### 対象範囲

- 姓名診断機能: ユーザーが既存の姓名（人間）を入力し、画数ベースのロジックで診断結果を表示する
- ペット命名提案機能: 犬・猫・小動物（うさぎ・ハムスター等）数種を対象に、苗字を含まない名前候補を提案する
- 提案時のオプション指定: 使いたい文字（must-include）、希望よみがな（逆引き生成）、出力文字種（漢字／ひらがな／カタカナ／アルファベット／なんでもいい）、対象動物、性別、希望カテゴリ

### 対象外・保留項目

| 対象外・保留項目 | 理由 | 再検討条件 |
|------------------|------|------------|
| 人間の赤ちゃん命名（新規名の提案） | 苗字を含めた五格計算との両立が必要になり規模が大きくなるため | 今回は完全に対象外（構想段階で決定済み） |
| アカウント・ログイン機能 | MVPをシンプルに保つため | 将来的に検討の可能性あり |
| 診断結果のDB保存（履歴機能） | 無料サービスでアカウント管理を増やさない方針 | URLパラメータ方式でのステートレス共有に加え、結果ファイル出力・クリップボードコピー（F-009, F-010）で代替 |
| 読み用マスタ（character_reading等）の新設 | kanjiapi.devのreading逆引きAPIで代替可能なため | API側の仕様変更・廃止時に再検討 |
| kanjiapi.dev障害時の実行時フォールバックAPI（jisho.org等） | 各APIの仕様・信頼性が未確認、実装コストに見合わない | kanjiapi.devの障害が実運用で頻発した場合に再検討（※Kanji alive／MuzukanjiAPIは「ビルド時の部首補正」用途では採用済み。ここで対象外なのは実行時のフォールバックとしての利用のみ） |
| 独自の名乗り読み（キラキラネーム的な読み）への対応 | 発生してからでないと定義できないため | 対象外（恒久） |

## 4. 用語定義

| 用語 | 定義 |
|------|------|
| 姓名判断 | 姓名の画数等から運勢・傾向を判定する伝統的な占い手法 |
| 五格（天格・人格・地格・外格・総格） | 姓名判断で使われる画数の集計区分。天格・人格・外格は姓を含む計算のため、苗字なしのペット候補では地格・総格のみを使用する |
| 熊崎式 | 姓名判断における代表的な流派の一つ。日本で最も普及している。部首によっては新字体の画数ではなく伝統的な部首画数（旧字体基準）を用いる独自の補正ルールを持つ（例: 氵→「水」の4画、扌→「手」の4画で数える） |
| 名乗り読み | 人名にのみ用いられる、辞書の通常の音訓読みとは異なる読み方 |
| モーラ | 日本語の音の単位（拍）。拗音・長音・促音は1モーラとして扱う |
| must-include | 「使いたい文字」機能における、結果に必ず含める文字の指定方式 |

## 5. 業務要件

- 姓名診断: ユーザーが姓を含む姓名を入力すると、都度 `character_master` の画数データを用いて五格を計算し、診断結果（総合点・ランク）を表示する。結果はDBに保存せず、URLパラメータに条件を埋め込むことで再計算・共有を可能にする。あわせて、ロジックで確定した診断結果をLLMに渡し、解説・肉付けコメントを生成する（F-012）。
- ペット命名提案: ユーザーが対象動物・性別・希望カテゴリ・使いたい文字・希望よみがな・出力文字種を指定すると、**ユーザー希望（希望よみ・使いたい文字）由来の候補をLLMで生成**し、不足分を `name_master` の条件一致候補からランダムに補って提示する。各候補は画数の吉凶・性別・カテゴリの合成スコア（性別の重みを特に重視）で評価する。**LLMが応答不可の場合はマスタのみで動作する**。

### 5.1 主要リソースのライフサイクル（該当時）

| リソース | 生成/取得 | 利用条件 | 更新 | 取消 | 上限 | 重複/競合 | 上限超過 | 消費/削除 | 復元/監査 |
|----------|-----------|----------|------|------|------|-----------|----------|-----------|-----------|
| 命名候補（name_master） | クローリング等で事前収集しCSV登録 | 対象動物・性別・カテゴリでフィルタ可能 | 管理者（開発者本人）のみ | 該当なし | 各動物種数百件程度を想定 | 同一名の重複登録は許容（表記違い等） | 該当なし | 削除しない（マスタデータ） | 該当なし（個人開発のため監査不要） |

## 6. 機能要件

### 6.1 機能一覧

| 機能ID | 機能名 | 概要 | 優先度 |
|--------|--------|------|--------|
| F-001 | 姓名診断 | 既存の姓名を入力し、都度計算で画数ベースの診断結果を表示する（name_masterは使用しない） | 必須（Phase1/MVP） |
| F-002 | ペット命名候補提案 | 対象動物・性別・カテゴリ・使いたい文字・希望よみがな・出力文字種の条件で候補をランキング表示する | 必須（Phase2） |
| F-003 | 使いたい文字指定 | **単体の1文字**を、名前の**表記（見た目の字）**に必ず含める。文字種は問わない（漢字・ひらがな・カタカナ・アルファベット）。かなも表記照合とし、希望のよみ（音の指定）と別物にする。**候補はLLMで生成**し、その字が本当に表記に含まれるかをロジック側で必ず機械検証する。マスタは組合せを賄いきれないためフォールバックとして残す。詳細設計は `docs/pet-usable-char.md` を正とする | 必須（Phase2）／**再定義・実装済（v2.4.0）** |
| F-004 | 希望よみからの表記生成 | 希望よみに対し、**LLMが自然な漢字表記を生成**する（先頭2件＋画数スコア上位を採用）。かな表記（ひらがな・カタカナ）も候補に含める。**当初のkanjiapi.dev読み逆引きによる機械的合成は非採用**（「丈人桜」のような不自然な並びを生んだため） | 必須（Phase2） |
| F-005 | 出力文字種指定 | 漢字のみ／ひらがな／カタカナ／アルファベット／なんでもいい、から選択可能にする | 必須（Phase2） |
| F-006 | 診断結果の共有 | 診断・提案条件をURLパラメータに埋め込み、ステートレスに再計算・共有可能にする | 必須（Phase1/MVP） |
| F-007 | 未知文字のAPIフォールバック取得 | character_masterに存在しない文字をkanjiapi.devから取得し、以後は character_master にキャッシュする | 必須（Phase1/MVP） |
| F-008 | アカウント機能 | ユーザー登録・ログイン | 対象外（将来検討） |
| F-009 | 結果ファイル出力 | 姓名診断・命名提案の結果をファイル（.txt）としてダウンロードできるようにする | 必須（Phase1/MVP）。命名提案側はPhase2 |
| F-010 | 結果クリップボードコピー | 姓名診断・命名提案の結果をワンクリックでクリップボードにコピーできるようにする | 必須（Phase1/MVP）。命名提案側はPhase2 |
| F-011 | 候補名へのLLMコメント生成 | 表示される命名候補すべてに対し、由来・意味も含めた説明文をLLMで自動生成する | 必須（Phase2） |
| F-012 | 姓名診断結果へのLLMコメント生成 | ロジックで計算した診断結果（五格・スコア・ランク・各格詳細・三才）を元に、LLMが解説・肉付けコメントを生成する。F-011と同じOllama→OpenRouterフォールバック基盤を共用する | 必須（Phase1/MVP） |
| F-013 | 性別による吉凶差の反映 | 性別（未指定／男性／女性）を入力として受け取り、女性の注意数（21・23・29・32・33・39画）に該当する格の吉凶を1段階引き下げ、スコア・解説に反映する。五格の計算自体は性別に影響しない。表現は伝統的見方と現代的見直しを併記する（設定値化） | 必須（Phase1/追加） |
| F-014 | 診断結果の詳細情報 | 各格の役割説明、1〜81画の象意キーワード・短評、三才配置（天・人・地の五行と相生・相剋）、四柱推命など他占術との連携用の五行サマリを診断結果に付与する（DB非保存・都度計算） | 必須（Phase1/追加） |
| F-015 | 四柱推命による五行ボーナス | 生年月日（任意入力）から四柱推命の考え方で**用神**（その人にとって吉となる五行）を求め、診断中の名前がそれを補えているかを「**四柱推命による五行ボーナス**」として**別枠で参考表示**する。評価は**星4段階**（★★★／★★☆／★☆☆／☆☆☆=恩恵なし）で、0〜100の数値は出さない。度合いのラベルは画面に出さず、名前を否定する文言は使わない。**総合ランク・スコア・五格は一切変えない**（共有URL(F-006)の再現性と姓名判断の一貫性を守るため。生年月日はURLに載せない）。出生時刻・出生地も任意で、未入力でも判定を進める（L1三柱→L2四柱→L3時差補正）。生年月日はDB・ログ・URL・LLMプロンプトに残さない。**ペット命名モードでは使わない**（ペットの生年月日は不明なことが多いため）。詳細設計は `docs/integration-shichu.md` を正とする | 必須（Phase3） |

> **MVP（Phase1）の対象**: F-001, F-006, F-007, F-009（診断結果）, F-010（診断結果）, F-012, F-013, F-014。**Phase2**: F-002〜F-005, F-011, F-009/F-010の命名提案側。**Phase3**: F-015（四柱推命連携）。フェーズ分割の詳細は基本設計（`docs/tasks.md`）参照。
> **実装状況（2026-07-18時点）**: Phase1（MVP）・Phase2（ペット命名提案）・**Phase3（F-015 四柱推命による五行ボーナス／Phase A=五行計算内蔵）** まで実装完了。本番は naming-service-red.vercel.app（全部Vercel構成）。現行バージョン `mvp-2.4.0`。F-015 の Phase B（四柱推命アプリのエンジンへ一元化）は未着手。

### 6.2 機能詳細（主要機能のみ）

**F-001 姓名診断**
- 入力: 姓・名（漢字／かな等）
- 処理: 入力文字を1文字ずつ `character_master` で画数変換し、五格（天格・人格・地格・外格・総格）を計算。`character_master` に文字がない場合は kanjiapi.dev に問い合わせ、取得結果を `character_master` にキャッシュする
- 出力: 総合点・ランク（SS〜C）・五格の内訳（F-012のLLMコメントを合わせて表示）
- **【確定・v1.8】流派**: 熊崎式を採用する。将来的に他の流派も選べるようにする方針のため、計算ロジックは流派を差し替え可能な構造（プラガブル）で実装する。ただしPhase1では熊崎式のみをハードコードし、複数流派管理用の `fortune_method` テーブルはまだ作らない（データ構造設計10章参照）
- **【確定・v1.7】スコア→ランク初期閾値**: SS=90以上／S=80〜89／A=65〜79／B=50〜64／C=49以下（実装中に調整可能な設定値として持つ）
- 例外: kanjiapi.devにも該当文字がない場合、診断不可の旨をエラー表示する。**【確定・v1.7】文言**:「一部の文字の画数情報を取得できませんでした。表記を変えて試すか、しばらくしてから再度お試しください。」

**F-012 姓名診断結果へのLLMコメント生成**
- 入力: F-001で計算済みの構造化データ（五格・スコア・ランク）
- 処理: ロジックで確定した診断結果をそのままLLMに渡し、結果の意味・傾向を解説する自然文コメントを生成する（ロジック側で結果を確定させ、LLMは「肉付け」役に徹する。診断結果そのものをLLMに計算させない）
- 提供元: F-011と同じくOllama（自宅サーバー）を第一候補、OpenRouterへフォールバック。フォールバック順・モデルも同じ環境変数で共用管理する
- 表示方法: 診断結果本体は即時表示し、LLMコメントは非同期で後から表示する（コメント生成に時間がかかっても、五格・スコア・ランクの表示は待たせない）【想定】
- 失敗時: Ollama・OpenRouterとも応答不可の場合、コメントなしで診断結果のみ表示する（F-011と同じフォールバック方針）

**F-002 ペット命名候補提案**
- 入力: 対象動物（犬／猫／小動物）、性別、希望カテゴリ、使いたい文字（任意・複数）、希望よみがな（任意）、出力文字種
- 処理: 希望よみ・使いたい文字があればLLMで候補を生成し（**指定文字がよみに含まれるか等をロジック側で必ず機械検証**）、不足分を `name_master` の条件一致候補からランダムに補う。各候補を総格スコア、カテゴリ、性別（重み大）の合成スコアで評価する。使いたい文字と出力文字種が矛盾する場合はエラー表示する。**画数が引けない文字を含む候補は除外する**
- 出力: スコア順の候補名リスト（各候補にF-011のLLMコメントを付与）

**F-009 結果ファイル出力**
- 対象: F-001（姓名診断結果）、F-002（命名候補リスト）
- 処理: 表示中の結果をテキスト（またはCSV等）としてブラウザ経由でダウンロードする。サーバー側にファイルを保存する必要はなく、クライアント側で生成する
- **【確定・v1.7】出力形式**: テキスト（.txt）のみとする。CSV／JSON等の対応は将来必要になれば追加する

**F-010 結果クリップボードコピー**
- 対象: F-001、F-002
- 処理: 表示中の結果（診断内容または候補名リスト）を1クリックでクリップボードにコピーする。ブラウザのClipboard APIを利用する想定

**F-011 候補名へのLLMコメント生成**
- 対象: F-002で表示される命名候補すべて
- 処理: 候補名ごとに、由来・意味も含めたやや長めの説明文をLLMで生成する
- 提供元: **Ollama（自宅サーバーを外部公開・接続実績あり）を第一候補とし、応答不可時はOpenRouterの無料枠モデルにフォールバックする**
- **フォールバック機能・フォールバック順の指定は、MVP時点から実装する**（後続フェーズへの先送りはしない）
- フォールバック順は設定で指定可能にする（デフォルトは `ollama → openrouter`。将来的に順序を入れ替えたり、片方のみを使う設定も可能にする）
- **使用モデルも設定で変更可能にする。** 初期想定モデルは以下の通り（両プロバイダともgemma4系で統一）
  - Ollama: `gemma4:31b-cloud`（Ollama Cloud経由）
  - OpenRouter: `google/gemma-4-31b-it:free`
- 各プロバイダともに無料利用のため、コスト・呼び出し回数は考慮不要とする
- **【想定】応答不可の判定基準**: タイムアウト5秒、またはHTTP 5xx／接続エラーを「応答不可」と判定し、次の候補（設定順の2番目）へフォールバックする。全プロバイダが応答不可の場合はコメントなしで候補名のみ表示する（リスク欄に対応済み）
- **【想定】設定方法**: 環境変数で管理する（個人開発・Vercel環境変数機能との親和性を優先し、設定画面は作らない）。例:
  - `LLM_FALLBACK_ORDER=ollama,openrouter`（カンマ区切りで順序を指定。`openrouter,ollama` や `ollama` 単独も可）
  - `LLM_OLLAMA_MODEL=gemma4:31b-cloud`
  - `LLM_OPENROUTER_MODEL=google/gemma-4-31b-it:free`

## 7. 非機能要件

| 観点 | 要件 |
|------|------|
| 性能 | 同時利用者数は小規模（個人サービス規模）を想定。画面表示は「診断中」等のプログレス表示を出せば10秒程度まで許容 |
| 可用性 | 【想定】個人運営のためSLAは設定しない。ベストエフォートでの提供とする |
| セキュリティ | ログイン機能なし、個人情報は収集しない。診断結果もDBに保存せずURLパラメータで共有する方式のため、個人情報保護上の懸念は小さい。将来アカウント機能を追加する場合は認証方式を別途検討する【要確認】 |
| 運用・保守 | 開発・運用は開発者本人のみが担当する。障害対応・問い合わせ対応も本人対応とする |
| 移植性・互換性 | スマートフォンを含むレスポンシブ対応とする（確定）。対象ブラウザはレンダリングエンジンChromium系（Chrome/Edge等）とWebKit系（Safari）の2系統を主対象とする |

## 8. データ要件

- データ構造の詳細は別紙「命名サービス向けデータ構造設計.md」（v11）を参照する。本書ではその要点のみ記載する。
- 提案用候補データ（`name_master`）は、各対象動物種あたり数百件程度の初期データ規模を想定する
- `character_master` は一般的な文字（常用漢字等）を網羅する方針とし、未収録の文字は kanjiapi.dev からの取得結果をキャッシュして拡充する
- 姓名診断（F-001）の結果はDBに保存しない。ペット命名候補（`name_master`, `fortune_result`, `fortune_detail`）のみ永続データとして保持する

## 9. 外部インターフェース・システム連携

| 連携先 | 方向 | 方式 | データ | タイミング |
|--------|------|------|--------|-----------|
| kanjiapi.dev | 取得 | REST API（`GET /v1/kanji/{character}`, `GET /v1/reading/{reading}`） | 文字の画数・読み情報 | 未知文字の都度、および読み逆引きの都度（ライブ呼び出し、事前キャッシュはstroke_countのみ） |
| Ollama（自宅サーバー、外部公開・接続実績あり） | 取得 | REST API（第一候補）。使用モデルは設定可能（初期想定: `gemma4:31b-cloud`） | 命名候補（F-002）の由来・意味を含む説明文（F-011）／姓名診断結果の解説コメント（F-012） | 命名候補・診断結果の表示時に生成（診断結果本体の表示はコメント生成を待たない） |
| OpenRouter API（無料枠モデル） | 取得 | REST API（Ollama応答不可時のフォールバック）。使用モデルは設定可能（初期想定: `google/gemma-4-31b-it:free`） | 同上 | Ollamaが応答不可の場合のみ |

## 10. 稼働環境・システム構成

| 項目 | 内容 |
|------|------|
| ホスティング（試験運用） | GitHub Pages（フロントエンド・静的） + Vercel（API・計算処理） |
| ホスティング（将来） | OCI（Oracle Cloud Infrastructure）のVPSへ移行予定 |
| ネットワーク | インターネット経由（一般公開の無料サービス） |
| 環境構成 | 【想定】本番環境のみ。検証環境は当面なし |

## 11. 制約条件・前提条件

- **費用**: 初期費用・運用費ともに基本0円を目標とする（無料枠のAPI・ホスティングのみ利用）
- **納期**: 特になし（趣味開発）
- **体制**: 開発・運用ともに開発者本人が一人で担当する
- **前提**: LLMはOllama（自宅サーバー、外部公開・接続実績あり）を第一候補とし、応答不可時はOpenRouterの無料枠モデルにフォールバックする。フォールバック順は設定可能とする

## 12. 成功基準・受入基準

- 受入基準: 開発者本人が実際に使用し、姓名診断・ペット命名提案の両機能が意図通りに動作すること
- 成功指標（KPI）: 【確定】定量的なKPIは設定しない。自分が使って機能すればよい、という基準とする

## 13. リスクと課題

| リスク/課題 | 影響 | 対応方針 |
|-------------|------|----------|
| kanjiapi.devの障害・レート制限 | 姓名診断・よみがな逆引き提案が一時的に利用不可になる | MVPではフォールバックAPIを実装せず、エラー表示のみで対応。実運用で障害が頻発する場合にフォールバック実装を再検討する |
| 独自の名乗り読みが未収録 | 一部の読み・文字で提案候補が出ない場合がある | 対象外として割り切り済み。診断不可時のメッセージ設計のみ検討する |
| 開発者一人での運用 | 多忙時・不在時に障害対応や保守が滞る可能性 | 個人開発の趣味プロジェクトとして許容する【想定】 |
| 自宅サーバー（Ollama）の可用性 | 自宅回線・サーバーの障害、停電、メンテナンス等でOllamaに接続できない期間が発生し得る | OpenRouterへのフォールバックで基本的に吸収する。両方とも応答不可の場合はコメントなしで候補名のみ表示する【想定】 |
| LLM出力の品質 | 説明文の内容が不自然、または事実と異なる由来を生成する可能性がある | 【想定】あくまで補助的な一言コメントとして扱い、正確性の保証はしない旨をUI上に軽く明記する |
| 熊崎式の部首補正とkanjiapi.devの画数不一致 | kanjiapi.devは伝統的な部首補正（氵→水4画等）を反映しない現代画数のため、未知文字を自動キャッシュした場合に熊崎式の正しい画数と異なる可能性がある | 補正が必要な代表的な部首・文字はシードデータ側で手動補正して`character_master`に投入する。Kanji alive API（第一候補）とMuzukanjiAPI（第二候補、Kanji alive対応外を埋める）の部首情報を使って対象文字の検出を半自動化する（データ構造設計v11参照）。それでも自動キャッシュされた未知文字は補正対象外となる限界を許容する【想定】 |

## 14. 要確認事項

### 未解決（実装時に確認・決定する）

| No | 確認事項 | 確認先/決定者 | 期限 | 次に決める工程 | 設計影響 |
|----|----------|---------------|------|----------------|----------|
| 4 | 熊崎式の「部首→補正値」対応表の作成と適用。**【実装済・v1.16】** 対応表（`radical_corrections.json`）＋オフラインパッケージ（kanji-data／kanji）で自動生成し、**常用＋人名用 約3,096字**のシードに部首補正を自動適用（約685字補正）。RapidAPIキーは不要になった。月(肉づき)のみ自動判別不可で手動確認対象 | gerupon | 完了 | 基本設計 | 済 |
| 5 | MuzukanjiAPIの実際のレスポンス形式・無料枠上限の検証（採用可否判断） | gerupon | T-003着手前 | 基本設計 | 後で可 |
| 6 | ローマ字出力時のかな変換・画数代用評価の具体的な変換ルール | gerupon | Phase2着手前 | 基本設計 | 後で可（Phase2機能） |
| 7 | 将来のアカウント機能導入時の認証方式 | gerupon | 導入検討時 | 将来の要件定義 | 後で可（現スコープ外） |

### 解消済み（記録用）

| 確認事項 | 決定内容 |
|----------|----------|
| 結果ファイル出力のフォーマット | 【v1.7】テキスト（.txt）のみ（6.2 F-009） |
| kanjiapi.dev障害時のリトライ・エラー文言 | 【v1.7】リトライ1回のみ→失敗時「現在、文字情報の取得に問題が発生しています。しばらくしてから再度お試しください。」 |
| kanjiapi.devにも文字がない場合のエラー文言 | 【v1.7】6.2 F-001に記載 |
| スコア→ランク閾値 | 【v1.7】SS=90+/S=80-89/A=65-79/B=50-64/C=49以下 → **【v1.15改定】6段階に変更**（下記） |
| 総合ランク（6段階・v1.15） | 大吉 ＞ 吉 ＞ 中吉 ＞ 小吉 ＞ 末吉 ＞ 凶。各下限を上から順に設定値化: 大吉90／吉76／中吉62／小吉48／末吉34／凶(34未満)。生々しい点数ではなく言葉を主表示にする |
| LLM応答不可の判定基準（旧No.1） | 【v1.15決定】タイムアウト**10秒**、またはHTTP **4xx／5xx**・接続エラーを応答不可と判定し次候補へフォールバック |
| フォールバック順・使用モデル（旧No.2） | 【v1.15決定】環境変数（`LLM_FALLBACK_ORDER`・各モデル）で設定する方式で確定 |
| 診断結果の即時表示＋コメント非同期（旧No.3） | 【v1.15決定】この方針で確定（動作確認済み。デザインは追って調整の可能性あり） |
| 姓名判断の流派 | 【v1.8】熊崎式（プラガブル構造、Phase1はハードコード） |
